#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    float x,y,z,u;
    cout<<"x = ";
    cin>>x;
		if (x<0){
			u = log(x*-1);
			cout<<u;
		}
		else u = pow(x,4) + 5;
			cout<<u;
	}
